# Supabase default config (public publishable keys, safe to commit)
# Set these so users can login without configuring their own Supabase project.
# Power users can override via the in-app config dialog.
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxx

# LLM proxy config (OpenAI-compatible endpoint, e.g. New API)
# Used by Netlify Edge Function: /api/llm/v1/chat/completions
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_API_KEY=sk-or-v1-your-openrouter-key
LLM_MODEL_ID=deepseek/deepseek-r1-0528:free

# Free model quota (applies only when users consume your hosted free model)
FREE_MODEL_ID=deepseek/deepseek-r1-0528:free
FREE_MODEL_DAILY_LIMIT=25
# Optional: custom quota cookie name
# FREE_MODEL_QUOTA_COOKIE_NAME=limerence_free_quota
# Optional: custom signature secret (defaults to LLM_API_KEY)
# FREE_MODEL_QUOTA_SECRET=replace-with-random-secret
